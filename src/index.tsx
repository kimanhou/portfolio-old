import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import AppRoot from './view/components/App/AppRoot';

ReactDOM.render(
  <AppRoot />,
  document.getElementById('root')
);
